job:
  range: "400000000000000000:7fffffffffffffffff"
  address: "1PWo3JeB9jrGwfHDNpdGK54CRas7fsVzXU"
  grid: "256,512"
  slices: 256
  random: false
  gpus: "0"
  solver: "bruteforce"          # bruteforce, kangaroo, rho, bsgs
  dp_bits: 20                  # Bits para Distinguished Points no Kangaroo (ex: 20)
  target_pubkey: ""            # Chave pública em hexadecimal (para Kangaroo/BSGS)

gpu:
  max_temp_c: 78

telemetry:
  interval: 1

api:
  port: 8080
  token: "cyclone_token_12345"

database:
  path: "./cyclone.db"

pool:
  role: "master"
  master_url: "http://localhost:8080"
  block_size_gkeys: 200
  random_in_block: false
